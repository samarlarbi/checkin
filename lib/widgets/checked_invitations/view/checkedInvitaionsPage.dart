import 'package:flutter/material.dart';

class CheckedInvitationsPage extends StatelessWidget {
  const CheckedInvitationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}